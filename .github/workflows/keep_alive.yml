name: Keep App Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:    # ← YEH ADD KAREIN (manual run ke liye)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: |
          curl -s -L "https://leveluts.streamlit.app/?auto_login=true&code=UTS-ADMIN001"
