name: Keep App Alive

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: |
          curl -s -L "https://leveluts.streamlit.app/?auto_login=true&code=UTS-ADMIN001"
